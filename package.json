{
  "name": "obsidian-mz-media-fetcher",
  "version": "0.2.0",
  "private": true,
  "description": "An Obsidian plugin that creates media notes from Bangumi and MobyGames with source-specific adapters.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "node --no-warnings --experimental-strip-types --experimental-specifier-resolution=node --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.4",
    "typescript": "^5.8.3"
  }
}
